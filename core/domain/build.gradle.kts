plugins {
    id("sweat.android.core")
    id("sweat.android.hilt")
}

android {
    namespace = "com.sweat.domain"
}

dependencies {
    // todo : Add Other Project Implementation -> ex) implementation(project(":core:___")) / (project(":feature:____"))
}