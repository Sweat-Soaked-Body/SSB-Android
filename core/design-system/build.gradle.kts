plugins {
    id("sweat.android.core")
    id("sweat.android.compose")
    id("sweat.android.lint")
}

android {
    namespace = "com.sweat.design_system"
}

dependencies {
    // Add Implementation
}